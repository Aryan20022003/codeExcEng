//this is firebase config , you will get this when you set firebase project
const config = {};

const storageBucket = "<something>.jappspot.com";
const databaseURL = "https:<something>.firebaseio.com";
const uploadDestination = "<upload folder in firebase>";

module.exports = { config, storageBucket, databaseURL, uploadDestination };
